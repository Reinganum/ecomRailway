# RecoverPassword
components:
  requestBodies:
    RecoverPassword:
      type: object
      required:
        - email
      properties:
        email:
          description: User's email address.
      example:
         email: benito.baranda@gmail.com